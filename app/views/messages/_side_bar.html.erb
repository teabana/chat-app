<div class="side_bar">
  <div class="side_bar__head">
    <div class="head_name">
      <%= link_to current_user.name, edit_user_path(current_user.id) %>
    </div>
    <div class="chat_link">
      <%= link_to "チャットを生成する", new_room_path %>
    </div>
  </div>
  <div class="side_bar__room">
    <% current_user.rooms.each do |room| %>
      <div class="chat_room">
        <a href="#"><%= room.name %></a>
      </div>
    <% end %>
  </div>
</div>